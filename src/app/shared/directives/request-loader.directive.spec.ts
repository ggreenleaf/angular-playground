import { RequestLoaderDirective } from './request-loader.directive';

describe('RequestLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new RequestLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
